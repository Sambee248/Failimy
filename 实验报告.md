# 数据库应用实践 — 家族谱系管理系统实验报告

## 一、业务场景

### 1.1 系统概述
本系统实现一个**多用户家族谱系管理平台**。用户可创建多个家族，每个家族包含若干成员。成员之间通过**亲子关系**和**婚姻关系**构成完整的血缘网络。

### 1.2 核心挑战
| 挑战 | 解决方案 |
|------|----------|
| 成员层级深度不确定 | 递归CTE（WITH RECURSIVE）动态计算 |
| 同名同姓成员识别 | 使用 member_id 唯一标识，支持搜索+筛选 |
| 直系亲属快速查询 | 数据库索引 + 递归CTE带深度限制 |
| 两人间血缘路径 | 双递归CTE求最近共同祖先 |

---

## 二、数据库建模

### 2.1 ER图

```
┌──────────┐       ┌──────────────┐       ┌──────────────┐
│  users   │       │   families   │       │   members    │
├──────────┤       ├──────────────┤       ├──────────────┤
│ user_id  │──1:N──│ user_id (FK) │──1:N──│ family_id(FK)│
│ username │       │ family_name  │       │ name         │
│ password │       │ description  │       │ gender       │
│ email    │       │ created_at   │       │ birth_date   │
│ created  │       └──────────────┘       │ death_date   │
└──────────┘                              │ generation   │
                                          │ biography    │
                      ┌───────────────────┤ created_by   │
                      │                   └──────────────┘
                      │                      ↑        ↑
                      │               parent_id   child_id
                      │                      │        │
                      │              ┌───────┴────────┴───────┐
                      │              │   family_relations      │
                      │              ├─────────────────────────┤
                      │              │ relation_id (PK)        │
                      │              │ parent_id (FK, INDEX)   │
                      │              │ child_id  (FK, INDEX)   │
                      │              │ relation_type           │
                      │              └─────────────────────────┘
                      │
                      │              ┌─────────────────────────┐
                      │              │      marriages          │
                      └──────────────├─────────────────────────┤
                                     │ marriage_id (PK)        │
                                     │ husband_id (FK, INDEX)  │
                                     │ wife_id    (FK, INDEX)  │
                                     │ marriage_date           │
                                     │ divorce_date            │
                                     └─────────────────────────┘
```

> 完整的 PlantUML 和 Mermaid 格式 ER 图见 `er_diagram.puml` 和 `er_diagram.mmd`

### 2.2 关系模式（3NF分析）

**users** (user_id, username, password_hash, email, created_at)
- 主键: user_id
- FD: user_id → {username, password_hash, email, created_at}
- 3NF: ✅ 无非主属性传递依赖

**families** (family_id, user_id, family_name, description, created_at)
- 主键: family_id
- FD: family_id → {user_id, family_name, description, created_at}
- 外键: user_id → users.user_id
- 3NF: ✅

**members** (member_id, family_id, name, gender, birth_date, death_date, generation, biography, created_by, created_at)
- 主键: member_id
- FD: member_id → {family_id, name, gender, birth_date, death_date, generation, biography, created_by, created_at}
- 外键: family_id → families.family_id, created_by → users.user_id
- 3NF: ✅ generation是直接属性，无传递依赖

**family_relations** (relation_id, parent_id, child_id, relation_type)
- 主键: relation_id
- UNIQUE(parent_id, child_id)
- FD: relation_id → {parent_id, child_id, relation_type}
- 外键: parent_id → members.member_id, child_id → members.member_id
- 3NF: ✅

**marriages** (marriage_id, husband_id, wife_id, marriage_date, divorce_date)
- 主键: marriage_id
- FD: marriage_id → {husband_id, wife_id, marriage_date, divorce_date}
- 外键: husband_id → members.member_id, wife_id → members.member_id
- 3NF: ✅

### 2.3 约束设计

| 约束类型 | 约束内容 | 实现方式 |
|----------|----------|----------|
| CHECK | gender IN ('M', 'F') | models.py + schema.sql |
| CHECK | death_date >= birth_date OR NULL | models.py + schema.sql |
| CHECK | relation_type IN ('biological', 'adopted') | models.py + schema.sql |
| CHECK | parent_id != child_id | models.py + schema.sql |
| CHECK | husband_id != wife_id | models.py + schema.sql |
| CHECK | divorce_date >= marriage_date OR NULL | models.py + schema.sql |
| UNIQUE | (parent_id, child_id) | models.py + schema.sql |
| 触发器 | parent.birth_date < child.birth_date | 应用层(relation_routes.py) + PostgreSQL触发器(schema.sql) |
| 触发器 | 防止循环关系（祖先=后代） | PostgreSQL触发器(schema.sql) + 应用层CTE预检 |

---

## 三、数据生成

### 3.1 模拟数据生成器

`generate_data.py` 支持多代家族数据生成：

- **家族数量**: 可配置（`--families N`），生成2+个家族，每个有独立姓氏
- **成员规模**: 可配置（`--members N`），支持单家族50,000+成员
- **生成逻辑**:
  - 第1代：2-4位创始成员（创始人+配偶）
  - 每代：2-5个子女/对夫妻，异性配对
  - 跨家族婚姻：后代与其他家族成员联姻
  - 最多20代，防止无限递归
  - 每5代批量提交，优化性能

```bash
python generate_data.py --families 2 --members 5000  # 2个家族，每族~5000成员
python generate_data.py --families 10 --members 50000 # 10个家族，总成员50万+
```

### 3.2 唐朝李唐皇族数据

`create_tang_family.py` 创建真实历史数据：

- 李虎 → 李昞 → **李渊（唐高祖）**→ **李世民（唐太宗）**→ 李治 → 武则天 → ... → 李柷（唐哀帝）
- 共41位历史人物，包含皇帝、皇后、公主、诗人（李白）
- 37条亲子关系、7段婚姻

---

## 四、SQL查询实现

### 4.1 递归CTE：祖先查询

```sql
WITH RECURSIVE ancestors AS (
    SELECT fr.parent_id, fr.child_id, fr.relation_type, 1 AS depth,
           ',' || fr.parent_id AS path
    FROM family_relations fr
    WHERE fr.child_id = :member_id
    UNION ALL
    SELECT fr.parent_id, fr.child_id, fr.relation_type, a.depth + 1,
           a.path || ',' || fr.parent_id
    FROM family_relations fr
    JOIN ancestors a ON fr.child_id = a.parent_id
    WHERE a.depth < :max_depth
      AND ',' || a.path || ',' NOT LIKE '%,' || fr.parent_id || ',%'
)
SELECT a.*, m.name, m.gender, m.birth_date, m.generation
FROM ancestors a JOIN members m ON m.member_id = a.parent_id
ORDER BY a.depth;
```

**防循环机制**：path字段记录访问路径，NOT LIKE条件防止环回。

### 4.2 递归CTE：后代查询

```sql
WITH RECURSIVE descendants AS (
    SELECT fr.parent_id, fr.child_id, fr.relation_type, 1 AS depth
    FROM family_relations fr
    WHERE fr.parent_id = :member_id
    UNION ALL
    SELECT fr.parent_id, fr.child_id, fr.relation_type, d.depth + 1
    FROM family_relations fr
    JOIN descendants d ON fr.parent_id = d.child_id
    WHERE d.depth < :max_depth
)
SELECT d.*, m.name, m.gender, m.birth_date, m.generation
FROM descendants d JOIN members m ON m.member_id = d.child_id
ORDER BY d.depth;
```

### 4.3 血缘路径查询

```sql
-- 步骤1：找到A和B所有祖先
WITH RECURSIVE ancestors_a AS (
    SELECT fr.parent_id, 1 AS depth FROM family_relations fr WHERE fr.child_id = :person_a
    UNION ALL
    SELECT fr.parent_id, a.depth + 1 FROM family_relations fr
    JOIN ancestors_a a ON fr.child_id = a.parent_id WHERE a.depth < 50
),
ancestors_b AS (
    SELECT fr.parent_id, 1 AS depth FROM family_relations fr WHERE fr.child_id = :person_b
    UNION ALL
    SELECT fr.parent_id, b.depth + 1 FROM family_relations fr
    JOIN ancestors_b b ON fr.child_id = b.parent_id WHERE b.depth < 50
)
-- 步骤2：找最近共同祖先
SELECT aa.parent_id AS common_ancestor,
       MIN(aa.depth) AS depth_a, MIN(ab.depth) AS depth_b
FROM ancestors_a aa JOIN ancestors_b ab ON aa.parent_id = ab.parent_id
GROUP BY aa.parent_id ORDER BY depth_a + depth_b LIMIT 1;
```

### 4.4 统计查询

**各代平均寿命**：
```sql
SELECT generation,
       ROUND(AVG(julianday(death_date) - julianday(birth_date)) / 365.25, 1)
FROM members WHERE family_id = :fid AND death_date IS NOT NULL
GROUP BY generation ORDER BY generation;
```

**50岁以上无配偶成员**：
```sql
SELECT m.*, CAST((julianday('now') - julianday(m.birth_date)) / 365.25 AS INTEGER) AS age
FROM members m WHERE m.family_id = :fid AND m.birth_date <= date('now', '-50 years')
AND m.death_date IS NULL AND m.member_id NOT IN (
    SELECT husband_id FROM marriages UNION SELECT wife_id FROM marriages
) ORDER BY age DESC;
```

---

## 五、索引与性能

### 5.1 索引设计

| 索引 | 目标查询 |
|------|----------|
| members.family_id | 家族成员列表 |
| members.name | 按姓名搜索 |
| members.gender | 性别筛选 |
| members.birth_date | 年龄统计、未婚长者查询 |
| members.generation | 世代筛选、统计分组 |
| family_relations.parent_id | 祖先递归CTE基准节点 |
| family_relations.child_id | 后代递归CTE基准节点 |
| marriages.husband_id / wife_id | 配偶查询、未婚长者排除 |

### 5.2 性能测试

`GET /api/families/{id}/query-ancestors?member_id=X&depth=5` 返回 `elapsed_ms` 字段。

SQLite环境下，5层深度祖先查询通常 < 5ms（小型数据集）到 < 50ms（大型数据集）。

---

## 六、系统实现

### 6.1 技术栈

| 层 | 技术 |
|----|------|
| 后端框架 | Flask 3.x |
| ORM | SQLAlchemy 2.0 |
| 数据库 | SQLite（可切换PostgreSQL） |
| 认证 | JWT（HS256, 24h过期） |
| 前端 | Bootstrap 5 + ECharts 5 + Vanilla JS |
| 密码 | bcrypt |

### 6.2 API端点总览（26个）

| 方法 | 路径 | 功能 |
|------|------|------|
| POST | /api/auth/register | 注册 |
| POST | /api/auth/login | 登录 |
| GET | /api/families | 家族列表 |
| POST | /api/families | 创建家族 |
| GET | /api/families/{id} | 家族详情 |
| PUT | /api/families/{id} | 编辑家族 |
| DELETE | /api/families/{id} | 删除家族 |
| GET | /api/members/families/{id} | 成员列表（搜索/过滤/分页） |
| POST | /api/members/families/{id} | 添加成员 |
| GET | /api/members/{id} | 成员详情（含关系） |
| PUT | /api/members/{id} | 编辑成员 |
| DELETE | /api/members/{id} | 删除成员 |
| GET | /api/members/search | 全用户搜索 |
| GET | /api/members/{id}/ancestors | 祖先查询（递归CTE） |
| GET | /api/members/{id}/descendants | 后代查询（递归CTE） |
| GET | /api/members/{id}/tree | 树结构（ECharts） |
| POST | /api/relations/parent-child | 添加亲子关系 |
| DELETE | /api/relations/parent-child/remove | 删除亲子关系 |
| POST | /api/relations/marriage | 添加婚姻 |
| DELETE | /api/relations/marriage/remove | 删除婚姻 |
| GET | /api/relations/path | 血缘路径查询 |
| GET | /api/dashboard | 仪表盘统计 |
| GET | /api/families/{id}/stats | 家族统计（世代+寿命） |
| GET | /api/families/{id}/unmarried-seniors | 50岁以上无配偶 |
| GET | /api/families/{id}/outliers | 数据异常检测 |
| GET | /api/families/{id}/export/csv | CSV导出 |
| GET | /api/families/{id}/export/full-csv | 完整CSV导出 |
| GET | /api/database/backup | 数据库备份下载 |
| GET | /api/families/{id}/query-ancestors | 查询性能分析 |
| GET | /api/families/{id}/query-path | 路径查询性能分析 |

### 6.3 前端页面（11个）

| 页面 | 功能 |
|------|------|
| login.html | 登录 |
| register.html | 注册 |
| dashboard.html | 仪表盘（统计卡片+性别饼图+快捷操作） |
| families.html | 家族管理（创建/编辑/删除/CSV导出） |
| member_list.html | 成员列表（搜索/性别过滤/世代过滤/分页） |
| member_form.html | 添加/编辑成员 |
| member_detail.html | 成员详情（信息+父母/配偶/子女/兄弟姐妹+家族树） |
| family_tree.html | 家族树（根成员选择/方向切换/深度调节/导出PNG） |
| relation_path.html | 血缘路径查询（双人选择+路径展示+关系图） |
| stats.html | 统计报表（世代分布+性别构成+平均寿命+异常检测） |

---

## 七、项目文件结构

```
family_genealogy/
├── app.py                  # Flask入口 + 前端路由
├── config.py               # 配置（SECRET_KEY, DATABASE_URL）
├── models.py               # 5表ORM模型 + 6个CHECK约束 + 12个索引
├── auth.py                 # JWT认证（创建/验证/login_required）
├── init_db.py              # 建表脚本
├── generate_data.py        # 模拟数据生成器（支持50,000+成员）
├── create_tang_family.py   # 李唐皇族数据生成
├── er_diagram.py           # ER图生成器（PlantUML + Mermaid + 3NF分析）
├── schema.sql              # PostgreSQL DDL（含索引+循环预防触发器+出生日期触发器）
├── requirements.txt        # Python依赖
├── routes/
│   ├── auth_routes.py      # 认证API
│   ├── family_routes.py    # 家族CRUD API
│   ├── member_routes.py    # 成员CRUD + 递归CTE + 树结构 + 搜索
│   ├── relation_routes.py  # 亲子关系 + 婚姻 + 血缘路径
│   ├── stats_routes.py     # 仪表盘 + 统计 + 未婚长者 + 异常检测
│   └── utility_routes.py   # CSV导出 + 数据库备份 + 查询性能分析
├── templates/              # 11个Jinja2前端页面
└── static/
    ├── css/shadcn.css      # Shadcn风格设计系统（597行）
    └── js/app.js           # 前端工具函数
```

---

## 八、测试

8个测试文件覆盖：API认证、CRUD操作、关系管理、递归查询、血缘路径、跨家族操作、分页搜索、边界条件、性能压力。

```bash
python test_api.py          # 33项API测试
python test_edge_cases.py   # 10项边界条件+性能测试
python test_relations.py    # 关系管理流程
python test_search.py       # 搜索功能
python test_cross_family.py # 跨家族操作
python test_frontend_flow.py# 前端完整流程
python test_final.py        # 兄弟姐妹+跨家族血缘路径
python test_full_flow.py    # 完整业务流程
```

---

## 九、数据库备份与恢复

```bash
# 在线下载备份（需登录）
curl "http://localhost:5000/api/database/backup?token=<JWT_TOKEN>" \
  -o genealogy_backup.db

# 本地备份
cp instance/genealogy.db genealogy_backup.db

# 恢复
cp genealogy_backup.db instance/genealogy.db
```

---

## 十、启动说明

```bash
cd family_genealogy
pip install -r requirements.txt
python init_db.py
python generate_data.py --families 2 --members 200
python app.py
# http://localhost:5000
# 测试账户: demo / demo123
# 李唐皇族: tang_li / tang123456
```
